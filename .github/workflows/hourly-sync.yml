name: ⚡ BTS Mission Hourly Sync

on:
  schedule:
    - cron: '0 * * * *'
  
  workflow_dispatch:
    inputs:
      week:
        description: 'Week label (leave empty for auto-detect)'
        required: false
        default: ''
      total_shards:
        description: 'Number of shards'
        required: false
        default: '5'
      skip_aggregation:
        description: 'Skip aggregation step'
        required: false
        default: 'false'
        type: boolean

env:
  EDGE_FUNCTION_URL: https://uspezooqcdrwaqxcqojn.supabase.co/functions/v1/spy-battle-api
  SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
  SYNC_ADMIN_KEY: ${{ secrets.SYNC_ADMIN_KEY }}

jobs:

  # ═══════════════════════════════════
  # STEP 1: Pre-flight check
  # ═══════════════════════════════════
  pre-sync:
    name: 🔍 Pre-Sync Check
    runs-on: ubuntu-latest
    outputs:
      current_week: ${{ steps.check.outputs.week }}
      total_shards: ${{ steps.check.outputs.shards }}
      should_sync: ${{ steps.check.outputs.should_sync }}
    steps:
      - name: Get Current Week
        id: check
        run: |
          RESPONSE=$(curl -s -X POST "$EDGE_FUNCTION_URL" \
            -H "Content-Type: application/json" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            --max-time 15 \
            -d '{"action": "getCurrentWeek"}')
          
          echo "📡 $RESPONSE"
          
          WEEK=$(echo "$RESPONSE" | jq -r '.week // empty')
          
          if [ -n "${{ github.event.inputs.week }}" ]; then
            WEEK="${{ github.event.inputs.week }}"
          fi
          
          SHARDS="${{ github.event.inputs.total_shards || '5' }}"
          
          if [ -z "$WEEK" ] || [ "$WEEK" = "Pre-Season" ]; then
            echo "⏸️ Pre-Season. Skipping."
            echo "should_sync=false" >> $GITHUB_OUTPUT
          else
            echo "✅ Week: $WEEK | Shards: $SHARDS"
            echo "should_sync=true" >> $GITHUB_OUTPUT
          fi
          
          echo "week=$WEEK" >> $GITHUB_OUTPUT
          echo "shards=$SHARDS" >> $GITHUB_OUTPUT

  # ═══════════════════════════════════
  # STEP 2: Parallel shard sync
  # 200 agents ÷ 5 shards = 40 each
  # ═══════════════════════════════════
  sync-shard:
    name: 🔄 Shard ${{ matrix.shard }}/5
    needs: pre-sync
    if: needs.pre-sync.outputs.should_sync == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        shard: [0, 1, 2, 3, 4]
      fail-fast: false
      max-parallel: 5
    steps:
      - name: Sync Shard ${{ matrix.shard }}
        run: |
          WEEK="${{ needs.pre-sync.outputs.current_week }}"
          SHARD=${{ matrix.shard }}
          SHARDS=${{ needs.pre-sync.outputs.total_shards }}
          START=$(date +%s)
          
          echo "🔄 Shard $SHARD/$SHARDS — $WEEK"
          
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "$EDGE_FUNCTION_URL" \
            -H "Content-Type: application/json" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            --max-time 120 \
            -d "{
              \"action\": \"initiateFairSyncShard\",
              \"adminKey\": \"$SYNC_ADMIN_KEY\",
              \"week\": \"$WEEK\",
              \"shard\": $SHARD,
              \"totalShards\": $SHARDS
            }")
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          END=$(date +%s)
          
          echo "📡 HTTP $HTTP_CODE ($(($END - $START))s)"
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          COMPLETED=$(echo "$BODY" | jq -r '.completed // 0')
          FAILED=$(echo "$BODY" | jq -r '.failed // 0')
          AGENTS=$(echo "$BODY" | jq -r '.agentsInShard // 0')
          DURATION=$(echo "$BODY" | jq -r '.duration // "?"')
          
          echo "┌──────────────────────────────┐"
          echo "│ Shard $SHARD Results          "
          echo "│ Agents:    $AGENTS            "
          echo "│ ✅ Done:   $COMPLETED         "
          echo "│ ❌ Failed: $FAILED            "
          echo "│ ⏱️  Time:  $DURATION          "
          echo "└──────────────────────────────┘"
          
          ERRORS=$(echo "$BODY" | jq -r '.errors[]? // empty' 2>/dev/null)
          if [ -n "$ERRORS" ]; then
            echo "⚠️ Errors:"
            echo "$ERRORS"
          fi
          
          if [ "$SUCCESS" != "true" ]; then
            echo "❌ FAILED: $(echo "$BODY" | jq -r '.error // "Unknown"')"
            exit 1
          fi

  # ═══════════════════════════════════
  # STEP 3: Aggregation (ONCE after all shards)
  # ═══════════════════════════════════
  aggregate:
    name: 📊 Aggregate
    needs: [pre-sync, sync-shard]
    if: |
      always() && 
      needs.pre-sync.outputs.should_sync == 'true' &&
      needs.sync-shard.result != 'failure' &&
      github.event.inputs.skip_aggregation != 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Wait for DB
        run: sleep 3

      - name: Run Aggregations
        run: |
          WEEK="${{ needs.pre-sync.outputs.current_week }}"
          echo "📊 Aggregating $WEEK..."
          
          RESPONSE=$(curl -s -X POST "$EDGE_FUNCTION_URL" \
            -H "Content-Type: application/json" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            --max-time 60 \
            -d "{
              \"action\": \"runAggregationOnly\",
              \"adminKey\": \"$SYNC_ADMIN_KEY\",
              \"week\": \"$WEEK\"
            }")
          
          echo "$RESPONSE" | jq .
          
          SUCCESS=$(echo "$RESPONSE" | jq -r '.success // false')
          if [ "$SUCCESS" = "true" ]; then
            echo "✅ Aggregations done!"
          else
            echo "⚠️ Aggregation issue"
            exit 1
          fi

  # ═══════════════════════════════════
  # STEP 4: Cleanup
  # ═══════════════════════════════════
  cleanup:
    name: 🧹 Cleanup
    needs: [pre-sync, aggregate]
    if: always() && needs.pre-sync.outputs.should_sync == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Remove Stale Users
        run: |
          RESPONSE=$(curl -s -X POST "$EDGE_FUNCTION_URL" \
            -H "Content-Type: application/json" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            --max-time 15 \
            -d '{"action": "cleanupOnlineUsers"}')
          
          echo "🧹 Removed $(echo "$RESPONSE" | jq -r '.removed // 0') stale users"

  # ═══════════════════════════════════
  # STEP 5: Summary
  # ═══════════════════════════════════
  summary:
    name: 📋 Summary
    needs: [pre-sync, sync-shard, aggregate, cleanup]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Final Report
        run: |
          echo ""
          echo "╔═══════════════════════════════════════════╗"
          echo "║       ⚡ BTS MISSION SYNC REPORT          ║"
          echo "╠═══════════════════════════════════════════╣"
          echo "║ 📅 Week:        ${{ needs.pre-sync.outputs.current_week }}"
          echo "║ 👥 Agents:      ~200"
          echo "║ 🔀 Shards:      ${{ needs.pre-sync.outputs.total_shards }}"
          echo "║ 🔄 Sync:        ${{ needs.sync-shard.result }}"
          echo "║ 📊 Aggregation: ${{ needs.aggregate.result }}"
          echo "║ 🧹 Cleanup:     ${{ needs.cleanup.result }}"
          echo "║ 🕐 Time:        $(date -u '+%H:%M:%S UTC')"
          echo "╚═══════════════════════════════════════════╝"
          echo ""
          
          if [ "${{ needs.sync-shard.result }}" = "success" ] && [ "${{ needs.aggregate.result }}" = "success" ]; then
            echo "🎉 All systems green!"
          elif [ "${{ needs.sync-shard.result }}" = "success" ]; then
            echo "⚠️ Sync OK but aggregation had issues"
          else
            echo "❌ Sync failures — check shard logs"
            exit 1
          fi
